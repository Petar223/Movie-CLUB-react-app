import React from "react";

import SideNavItems from "./navButtons";
const Navigation = () => {
  return (
    <nav class="sidebar">
      <ul class="side-nav">
        <SideNavItems />
      </ul>
    </nav>
  );
};

export default Navigation;
